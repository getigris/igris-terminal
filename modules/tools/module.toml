[module]
name = "tools"
description = "Modern CLI replacements: bat, lsd, fzf, zoxide, mole; optional: igris-memory"
version = "1.1.0"
default = true

[dependencies]
requires = []
tools = []

[platforms]
supported = ["macos", "linux"]
